# Анализ переменных окружения

## ✅ Обязательные переменные (для работы бота)

### 1. `BOT_TOKEN` ⭐⭐⭐
**Использование:** Строка 94 в AccountantBot.java
**Статус:** **ОБЯЗАТЕЛЬНА**
**Логика:** ✅ Правильно - без токена бот не может работать
**Дефолт:** Нет (есть в config.properties, но лучше через env)

---

### 2. `BOT_USERNAME` ⭐⭐
**Использование:** Строка 95 в AccountantBot.java
**Статус:** **ОБЯЗАТЕЛЬНА** (но есть дефолт)
**Логика:** ⚠️ Можно улучшить - есть дефолт "accountant_bot" в коде
**Рекомендация:** Оставить обязательной для явности, но можно не указывать если username = "accountant_bot"

---

### 3. `DATA_DIR` ⭐⭐⭐
**Использование:** 
- Строка 98 в AccountantBot.java
- Строка 40 в MonthlyReportScheduler.java
- Строка 29 в RecurringExpenseScheduler.java
**Статус:** **ОБЯЗАТЕЛЬНА для Railway** (есть дефолт "data")
**Логика:** ✅ Правильно - должен совпадать с mount path Volume
**Дефолт:** "data" (но для Railway нужно "/data")

---

### 4. `PORT` ⭐⭐
**Использование:** Строка 23 в Main.java
**Статус:** **ОПЦИОНАЛЬНА** (Railway устанавливает автоматически)
**Логика:** ⚠️ Railway обычно устанавливает автоматически, но лучше указать явно
**Дефолт:** 10000
**Рекомендация:** Оставить в документации, но пометить как "Railway установит автоматически, но можно указать явно"

---

### 5. `KEEP_ALIVE_MODE` ⭐⭐
**Использование:** Строка 234 в AccountantBot.java
**Статус:** **ОПЦИОНАЛЬНА** (но рекомендуется)
**Логика:** ✅ Правильно - есть дефолт "always" в коде
**Дефолт:** "always" (если не указано)
**Рекомендация:** Оставить в документации как рекомендуемую, но не обязательную

---

## ⚙️ Опциональные переменные (для Google Sheets)

### 6. `GOOGLE_CREDENTIALS` ⭐⭐⭐
**Использование:** Строка 121 в AccountantBot.java
**Статус:** **ОПЦИОНАЛЬНА** (но рекомендуется для Railway)
**Логика:** ✅ Правильно - приоритет над GOOGLE_CREDENTIALS_PATH
**Рекомендация:** ✅ Оставить как есть - лучший вариант для Railway

---

### 7. `GOOGLE_CREDENTIALS_PATH` ⭐⭐
**Использование:** Строка 120 в AccountantBot.java
**Статус:** **ОПЦИОНАЛЬНА** (альтернатива GOOGLE_CREDENTIALS)
**Логика:** ✅ Правильно - используется если GOOGLE_CREDENTIALS не указана
**Рекомендация:** ✅ Оставить как альтернативу, но пометить что GOOGLE_CREDENTIALS предпочтительнее

---

### 8. `GOOGLE_SPREADSHEET_ID` ⭐
**Использование:** Строка 164 в AccountantBot.java
**Статус:** **ОПЦИОНАЛЬНА** (только для дефолтной таблицы)
**Логика:** ✅ Правильно - используется только как fallback
**Рекомендация:** ✅ Уже правильно помечено как опциональная

---

### 9. `GOOGLE_SHEET_NAME` ⭐⭐
**Использование:** Строка 165 в AccountantBot.java
**Статус:** **ОПЦИОНАЛЬНА** (есть дефолт)
**Логика:** ✅ Правильно - есть дефолт "Sheet1"
**Дефолт:** "Sheet1"
**Рекомендация:** ✅ Оставить как опциональную

---

## ❌ Переменные, которые можно убрать/упростить

### 10. `STORAGE_CSV_PATH` ⭐
**Использование:** Строка 104 в AccountantBot.java
**Статус:** **ИЗБЫТОЧНА**
**Логика:** ❌ Формируется автоматически из `DATA_DIR + "/expenses.csv"`
**Проблема:** Дублирует функциональность DATA_DIR
**Рекомендация:** 
- ⚠️ Можно убрать из документации как обязательную
- Оставить в коде для гибкости (если кому-то нужен другой путь)
- В документации пометить как "Не требуется, формируется автоматически"

---

## 📊 Итоговая оценка

### Обязательные (минимум для работы):
1. ✅ `BOT_TOKEN` - обязательна
2. ✅ `BOT_USERNAME` - обязательна (но есть дефолт)
3. ✅ `DATA_DIR` - обязательна для Railway (есть дефолт для локального запуска)

### Рекомендуемые (для production):
4. ✅ `PORT` - Railway установит автоматически, но лучше указать явно
5. ✅ `KEEP_ALIVE_MODE=always` - рекомендуется для 24/7 работы

### Опциональные (для Google Sheets):
6. ✅ `GOOGLE_CREDENTIALS` - рекомендуется если используете Google Sheets
7. ✅ `GOOGLE_CREDENTIALS_PATH` - альтернатива (менее предпочтительна)
8. ✅ `GOOGLE_SPREADSHEET_ID` - опциональна (только для fallback)
9. ✅ `GOOGLE_SHEET_NAME` - опциональна (есть дефолт "Sheet1")

### Избыточные:
10. ⚠️ `STORAGE_CSV_PATH` - избыточна, формируется автоматически

---

## 🔧 Рекомендации по улучшению документации

1. ✅ Убрать `STORAGE_CSV_PATH` из основных переменных (оставить только в разделе "Дополнительные")
2. ✅ Пометить `PORT` как "Railway установит автоматически, но можно указать явно"
3. ✅ Пометить `KEEP_ALIVE_MODE` как "Рекомендуется, но не обязательно (дефолт: always)"
4. ✅ Уточнить что `BOT_USERNAME` можно не указывать если username = "accountant_bot"
5. ✅ Подчеркнуть что `GOOGLE_CREDENTIALS` предпочтительнее `GOOGLE_CREDENTIALS_PATH` для Railway

---

## ✅ Вывод

Все переменные используются логично и правильно. Единственное улучшение:
- **STORAGE_CSV_PATH** - избыточна, можно убрать из основной документации или пометить как "Не требуется"

Остальные переменные на месте и используются правильно.

